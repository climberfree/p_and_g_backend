CREATE TABLE IF NOT EXISTS ANIMAL (
                                      id BIGINT AUTO_INCREMENT PRIMARY KEY,
                                      name VARCHAR(255),
    species VARCHAR(255),
    age INT
    );
